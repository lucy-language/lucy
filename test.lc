%"c" @linux_x86 @macos_x64;

pkg test;

ext int printf(str fmt, var str args);

struct Person
{
    Name name;
    int age;
}

struct Name
{
    str first_name;
    str last_name;
}

def int main(str[] args)
{
    var Person p1 =
    {
        {
            "Kiana",
            "Bennett"
        },
        22
    };

    printf("%s\n", p1.name.first_name);
    
    return 0;
}

# TODO
#macro void printn(x)
#{
#    printf("%s", x);
#}
