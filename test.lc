pkg test;

ext void  printf(str fmt, var str args);
ext void* malloc(int size);

struct User
{
    str username;
    str email;
}

def int main()
{
    var User* user = malloc(User.$size);
    
    return 0;
}
