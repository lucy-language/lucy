pkg test;

def void main()
{
    var (str, int, bool) name, age, dev = test();
}

def (str, int, bool) test()
{
    return "Kiana", 22, true;
}

# @.str = global [6 x i8] c"Kiana\00"
#
# define i64 @test.main() {
# entry:
#   %0 = call { ptr, i64, i1 } @test.test()
#   %name = alloca ptr, align 8
#   %1 = extractvalue { ptr, i64, i1 } %0, 0
#   store ptr %1, ptr %name, align 8
#   %age = alloca i64, align 8
#   %2 = extractvalue { ptr, i64, i1 } %0, 1
#   store i64 %2, ptr %age, align 4
#   %dev = alloca i1, align 1
#   %3 = extractvalue { ptr, i64, i1 } %0, 2
#   store i1 %3, ptr %dev, align 1
#   ret i64 0
# }
#
# define { ptr, i64, i1 } @test.test() {
# entry:
#   ret { ptr, i64, i1 } { ptr @.str, i64 22, i1 true }
# }
